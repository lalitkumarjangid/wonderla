/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'www.pexels.com',
            'images.unsplash.com',
            'videos.pexels.com',
            'd22pimhl2qmbj7.cloudfront.net'
        ]
    }
};

export default nextConfig;
